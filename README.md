# githook.sh

many people use husky to manage git hooks.

but husky has an npm dependency.

so this is one that attempts to not have too many dependencies.

it's a single shell script that basically does what husky does

this is a repo for both the script and the site
