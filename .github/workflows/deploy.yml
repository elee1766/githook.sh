name: deploy

on:
  release:
    types: [published]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: trigger render deploy
        run: curl -X POST "${{ secrets.RENDER_DEPLOY_HOOK }}"
